pipeline {
    agent any

    stages {
        stage('DEBUG') {
            steps {
                script {
                    sh "ECHO BRANCH-2"
                }

            }
        }
    }
}

